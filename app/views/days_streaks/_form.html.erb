<%= form_with(model: days_streak) do |form| %>
  <% if days_streak.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(days_streak.errors.count, "error") %> prohibited this days_streak from being saved:</h2>

      <ul>
        <% days_streak.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :count, style: "display: block" %>
    <%= form.number_field :count %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
