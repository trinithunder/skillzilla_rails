<%= form_with(model: learning_style) do |form| %>
  <% if learning_style.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(learning_style.errors.count, "error") %> prohibited this learning_style from being saved:</h2>

      <ul>
        <% learning_style.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :message, style: "display: block" %>
    <%= form.text_area :message %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
