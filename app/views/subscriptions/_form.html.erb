<%= form_with(model: subscription) do |form| %>
  <% if subscription.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(subscription.errors.count, "error") %> prohibited this subscription from being saved:</h2>

      <ul>
        <% subscription.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :plan_name, style: "display: block" %>
    <%= form.text_field :plan_name %>
  </div>

  <div>
    <%= form.label :plan_price, style: "display: block" %>
    <%= form.number_field :plan_price %>
  </div>

  <div>
    <%= form.label :plan_billing_date, style: "display: block" %>
    <%= form.date_field :plan_billing_date %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
