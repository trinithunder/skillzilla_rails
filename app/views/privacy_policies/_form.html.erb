<%= form_with(model: privacy_policy) do |form| %>
  <% if privacy_policy.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(privacy_policy.errors.count, "error") %> prohibited this privacy_policy from being saved:</h2>

      <ul>
        <% privacy_policy.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :message, style: "display: block" %>
    <%= form.text_area :message %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
