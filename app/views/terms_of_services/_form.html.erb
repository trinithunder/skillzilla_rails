<%= form_with(model: terms_of_service) do |form| %>
  <% if terms_of_service.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(terms_of_service.errors.count, "error") %> prohibited this terms_of_service from being saved:</h2>

      <ul>
        <% terms_of_service.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :message, style: "display: block" %>
    <%= form.text_area :message %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
