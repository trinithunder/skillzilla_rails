<%= form_with(model: app_layer) do |form| %>
  <% if app_layer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(app_layer.errors.count, "error") %> prohibited this app_layer from being saved:</h2>

      <ul>
        <% app_layer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
