<%= form_with(model: topic_comment) do |form| %>
  <% if topic_comment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(topic_comment.errors.count, "error") %> prohibited this topic_comment from being saved:</h2>

      <ul>
        <% topic_comment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_area :body %>
  </div>

  <div>
    <%= form.label :topic_id, style: "display: block" %>
    <%= form.text_field :topic_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
